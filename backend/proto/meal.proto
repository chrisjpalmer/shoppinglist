syntax = "proto3";

message Meal {
    int32 id = 1;
    string name = 2;
    repeated IngredientRef ingredient_refs = 3;
}

message IngredientRef {
    int32 ingredient_id = 1;
    int32 number = 2;
}