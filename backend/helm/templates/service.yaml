apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    {{- include "backend.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
      nodePort: 30001
  selector:
    app: backend
